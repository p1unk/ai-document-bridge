version: '3.8'
services:
  ai-bridge:
    image: p1unknown/ai-bridge:latest
    container_name: ai-bridge
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"